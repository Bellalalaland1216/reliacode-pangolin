# 穿山甲·溯源大师 UI 设计规则协议

版本：1.5 · 更新日期：2026-09-20

适用项目：穿山甲·溯源大师 / ReliaCode

依据：本轮对话中用户对登录、普通用户注册、合作方注册及商品溯源入口的要求与纠正。

本文件用于延续用户已明确表达的审美、使用习惯和修改边界。后续进行 UI 设计、前端实现、动画调整、响应式适配或视觉修复前，应先阅读本文件，并查看当前任务对应的参考图。不能用本协议代替用户的最新指示，也不能以“优化设计”为由扩大修改范围。

## 1. 要求的适用顺序

1. 用户最新明确指示优先；同一问题以最近一次修正为准。
2. 对应页面已确认的参考图和已验收效果，是该页面的视觉基准。
3. 本协议中的通用偏好用于补足未说明的细节，不能覆盖页面已有的专门要求。
4. 当前代码、旧截图、历史说明或开发者个人判断，与上述要求冲突时，不能作为继续保留错误的理由。

用户反复强调的核心是：**严格参考、只改指定内容、比例协调、单屏完整、真实交互、动画自然。**

## 2. 修改方式与协作习惯

- 按页面逐步推进。本轮指定哪个页面、哪个部位，就围绕该范围完成修改。
- “其他地方都很好”“只改这一处”“没提到的不要动”是明确的范围限制。不得顺手替换背景、素材、字体体系、布局、文案、动画或业务流程。
- 已被用户认可的版本应作为后续修复基线。修正某个轮子、草的摆动或图层接缝，不应导致整张插画再次变化。
- UI 调整默认保留功能和业务逻辑；用户明确要求调整字段或跳转流程时，才实施对应改动。
- 参考图由生图模型生成时，要修正其中不合理的文字、重复元素或无法工作的控件。变通的目的是让页面可用，不能借机换一种设计。
- 已明确的要求直接执行，不反复要求用户确认同一件事；只有影响结果且无法从上下文确定的歧义才需要澄清。
- 不把某次修复的实现方式当成永久设计目标；最终判断依据是用户看到的效果与实际操作结果。

## 3. 视觉风格与素材忠实度

### 3.1 已确认的整体风格

- 薄荷绿、浅青绿的明亮背景，深绿色的主要操作和文字，白色或半透明的面板。
- 植物、叶片、草、白色小花、柔和光影构成自然环境；物流车、包装箱、爪印、虚线运输路径体现产品语境。
- 画面柔和、有留白，控件清晰，细节精致。不能通过把所有元素放大来制造“醒目”。
- 卡片边界、圆角、阴影、透明感应协调，延续对应参考图的质感。

这些是对已确认参考图的归纳，不是重新创作插画的许可。

### 3.2 必须保留的参考细节

- 对照原图保留主体位置、相互比例、白色卡片大小、左右分区、留白、背景构图和元素层次。
- 卡片之外的背景同样属于设计要求，不能只还原白框里面的内容。
- 草和植物的位置、密度、形态应沿用原图；不能变成另一种草、移走一片植物或留下突兀的空地。
- 包装箱沿用原图的大小、透视、颜色、爪印、二维码标签和搬运标识，不能换成另一只外观不同的箱子。
- 车辆沿用已确认的外形、配色和细节。动画需要补齐遮挡部分时，应与原车衔接自然，不能换车型。
- 优先复用已确认素材和图层。仅为制作动画而分层，不应导致原画面被重画或丢失细节。
- 不用一张带有完整表单的截图冒充网页；插画可以用图片，输入框和操作入口必须是真实控件。

## 4. 比例、字号与单屏适配

### 4.1 控制视觉密度

- 注册卡片、切换按钮、输入框、主按钮和文字都应克制，避免中间面板占满画面、字号过大、控件粗重的效果。
- 调整时同时考虑卡片占屏比例、内边距、行距、控件高度、字体大小和周围留白，不能只缩某一个元素。
- 结合人眼观感和黄金分割思路组织主次与留白。用户没有要求每个尺寸必须等于固定的 1.618，也没有指定全站统一像素值；不得机械套比例。
- 参考图的协调感与真实屏幕上的可读、可点同等重要。不能通过把整页缩到文字难以辨认来“塞进一屏”。

### 4.2 单屏页面不得出现页面级拖动

- 登录、普通用户注册、合作方注册、商品溯源查询入口等本应完整展示在一个界面内的页面，应在当前可用视口内完整呈现。
- 不出现页面级上下滚动，也不产生横向溢出。登录页同样适用，不能作为例外。
- 同时依据可用宽度与高度适配，考虑浏览器工具栏、系统缩放和窗口缩放后的实际视口。
- 默认状态、错误提示、按钮加载及必要内容展开后，输入框、主要操作和返回入口仍应可见、可用。
- 不得仅设置 `overflow: hidden` 或裁掉底部内容来掩盖溢出；应调整排版、间距、密度，必要时采用紧凑布局或多列布局。
- 保留满幅背景，不能把整个设计缩进容器后留下两侧空白竖条；也不能以修复滚动为由改成另一套登录设计。
- 避免明显拉伸主体、裁掉关键插画或遮住表单来填满屏幕。背景铺满、插画呈现和表单适配需协调处理。

适用范围说明：用户禁止的是“只有一个界面”的页面拖动，不应据此推导出所有长列表、长详情也必须强塞进一屏。长内容按其页面任务处理，不能反过来破坏入口页的单屏要求。

## 5. 液态玻璃要求

- 普通用户注册与合作方注册的输入框必须具有**明显的液态玻璃质感**，不能只做白色填充加一条浅色边框。
- 通过透光层次、边缘高光、柔和折射感、背景透映和适度阴影形成质感；具体技术手段由实现决定。
- 表单文字、占位文字、输入值和焦点状态应清晰，玻璃效果不能遮住内容或降低操作辨识度。
- 卡片与绿色按钮的质感应与参考图协调，整体仍需轻盈，不能变成厚重的大块装饰。
- 这项明确要求源自两个注册页面。登录页继续保留用户确认的原样式，不能未经要求将注册页的整套玻璃布局套过去。
- 商品溯源入口沿用用户指定的同风格底图，控件风格与该系列协调。

## 6. 文案与真实交互

- 登录页明确执行“只要有字，就应有可点开、可操作的交互”。小字入口也不能只是装饰。
- 字段标签可点击聚焦对应输入框；保持登录文案可切换复选框；显示密码、注册、帮助、商品溯源等文字应触发对应功能。
- 标题或标识需要交互时，应绑定合理行为，不能只有鼠标手型或一个没有效果的点击事件。
- 不添加无用的第三人称叙述、装饰性说明、与操作无关的提示文案，尤其不能照搬生图里没有实际用途的小字。
- 必要的字段名称、输入格式和错误反馈应直接、简洁、与当前操作相关；不能为了“少文字”删除必要反馈。
- 避免为了“所有字都能点”擅自新建页面或无意义入口。优先使用聚焦、切换、打开实际帮助等现有合理行为。
- 此条是登录页的明确要求；其余入口页延续“文案有用途、入口有功能”的原则，不能无依据扩展为所有详情正文都必须做成链接。

执行解释：插画中箱体印刷、车辆字样、装饰二维码属于原图素材，不应伪装成可查询的真实业务入口。实际 UI 文字与装饰图案应有清楚的语义区别。

## 7. 登录页动画规则

动画的作用是让原图中指定元素动起来，而不是改画面。正常浏览、鼠标点击、聚焦和输入时，动画必须持续，不能因点击页面就停止。

| 元素 | 已确认的行为 | 必须避免的问题 |
| --- | --- | --- |
| 卡车 | 一辆接一辆，节奏适中、速度不要太快；从绿色狗形状后方自然开出，驶向右侧，并在原图指定的右侧边界消失 | 突然闪现、过快穿过、机械跳跃、从竖直矩形切口冒出 |
| 遮挡关系 | 使用绿色狗的真实轮廓和原有前景植物遮挡车辆，出入画自然 | 像存在一堵无形的竖直墙，车身被生硬切断 |
| 前后轮 | 轮胎完整、位置和透视正确；车轮随车辆行进转动，呈现真实行驶感 | 少轮子、轮缘缺块、轮子静止、轮心漂移或转动时轮胎变形 |
| 小位置标 | 沿原图虚线从运输起点移动至上方定位终点，与卡车速度和运输过程同步 | 偏离虚线、各自乱动、快慢与车辆脱节 |
| 包装箱 | 每辆车经过触发一次抖动，呈现里面像有东西要跳出来的感觉；有上下左右摆动，幅度清楚但自然 | 真的冒出物体、换掉原箱子、抖动弱到看不出或夸张失控 |
| 叶片 | 页面叶片呈现有规律、有美感的飘落，节奏错开，衔接自然 | 静止大片叶子与需求不符、杂乱飞舞、瞬移或图层边缘露出 |
| 草与植物 | 草随风轻微摇摆，尤其包括卡片内左下方前景植物及白框外左侧植物 | 漏掉用户指出的两处植物、改变草的样子和位置、摆动幅度过大 |

用户已要求包装箱抖动幅度比早期版本稍大，后续应沿用已认可的效果，不能又退回几乎不可见的状态。具体时长与幅度以已确认效果为基线，不把某次实现参数当成用户指定的永久数值。

动画适配系统减少动态效果偏好、后台页面生命周期时，应与正常交互区分，不能把点击、输入、勾选或弹窗操作误当成暂停条件。

## 8. 各页面已确认的功能与呈现约定

| 页面或入口 | 应延续的约定 |
| --- | --- |
| 登录页 | 保留已确认的原图构图、白色卡片、外侧背景与运输动画；账号、密码、保持登录、显示密码、注册、帮助和商品溯源入口正常工作 |
| 登录页“邀请码注册” | 点击后先进入普通用户注册界面；用户再点击“合作方注册”，才切换至合作方注册界面。不能仅凭入口文案默认打开合作方页 |
| 普通用户注册 | 登录账号使用手机号或邮箱；保留姓名或昵称、密码、确认密码；去掉单独的“手机号（选填）”字段，并重新平衡排版 |
| 合作方注册 | 保留普通用户/合作方切换；按参考图显示邀请码输入与“验证邀请码”，保持原有邀请码注册流程与返回登录入口 |
| 商品溯源入口 | 从登录页点击商品溯源进入后，使用用户指定的薄荷绿植物、包装箱底图，延续该系列风格；保留实际查询和已有相关操作 |

“手机号或邮箱登录”指已明确要求的账号形式，不代表用户同时要求新增短信验证码、邮箱验证码、第三方登录或额外注册步骤。已有账号兼容性与认证能力不能因视觉调整而被破坏。

## 9. 原图与已确认素材索引

源图保存在本工作区的 `../UI设计样图/`。以下文件名对应当前整理所依据的版本；用户提供新图时，应更新对应项，不能继续引用已被替代的旧图。

| 用途 | 原图 |
| --- | --- |
| 登录页完整构图 | [ChatGPT Image 2026年9月16日 11_52_08.png](<../UI设计样图/ChatGPT Image 2026年9月16日 11_52_08.png>) |
| 合作方注册参考 | [ChatGPT Image 2026年9月17日 18_50_04.png](<../UI设计样图/ChatGPT Image 2026年9月17日 18_50_04.png>) |
| 普通用户注册参考 | [ChatGPT Image 2026年9月17日 19_15_15.png](<../UI设计样图/ChatGPT Image 2026年9月17日 19_15_15.png>) |
| 商品溯源入口底图 | [ChatGPT Image 2026年9月17日 21_03_35.png](<../UI设计样图/ChatGPT Image 2026年9月17日 21_03_35.png>) |

登录原图画布为 1586 × 992，白色卡片参考位置为 `(185, 79)`、参考大小为 `1216 × 806`。这些数据用于核对原图构图，不意味着所有屏幕固定使用这些像素尺寸。

仓库内登录素材说明位于 [public/images/login-woodland/README.md](public/images/login-woodland/README.md)。原始参考图属于工作区设计资料，不能假设单独克隆仓库后也自动包含这些外部文件。

## 10. 必须防止再次出现的视觉回归

- 宽窗口出现左右空白竖条，或背景缩在中间。
- 图层缩放不一致，出现重复背景、矩形拼接边缘、文字碎片或按钮残影。
- 把 SVG 的 `viewBox` 当成完整裁切措施，导致原图中本应隐藏的部分在其他窗口比例下露出。
- 卡片、插画、外部背景使用不一致的坐标映射，造成边缘错位；修复时应同时核对裁切范围、缩放和圆角衔接。
- 解决单屏问题后，卡片比例、插画形态或布局与已确认设计明显不同。
- 表单放大后需要滚动，或者通过隐藏溢出来裁掉返回入口和错误提示。
- 动画分层破坏草、箱子、车轮等已确认细节，或点击页面导致动画停止。
- 使用“已检查 DOM 尺寸”代替看实际截图。没有滚动条不等于视觉正确。

## 11. 完成与验收约定

每次 UI 变更完成前，按本次改动范围核对以下事项：

- [ ] 明确本次指定的页面和部位，未混入其他页面重设计或无关功能改动。
- [ ] 对照对应原图查看实际渲染效果，包括白框内外、比例、字号、间距和素材细节。
- [ ] 检查桌面、宽屏、笔记本、缩放后的小视口以及手机横竖屏；单屏入口没有页面级滚动，关键控件完整可用。
- [ ] 检查默认、空输入报错、服务错误及本次涉及的展开状态，不能只验收空白表单。
- [ ] 涉及玻璃质感时，确认输入框具有明显质感且文字清晰。
- [ ] 点击实际入口、字段标签、切换项和按钮，确认它们有正确行为；涉及流程时核对跳转顺序。
- [ ] 涉及动画或插画布局时，检查车辆出入画、前后轮、定位标、包装箱、草与叶片，并在点击和输入过程中观察是否持续。
- [ ] 检查不同窗口比例及动画阶段的裁切边缘，确认没有竖条、重复图层或残片。
- [ ] 说明实际改了什么、怎样验证，以及还有哪些未完成的限制；不能把未验证的效果称为已完成。
- [ ] 按项目 Git 约定记录提交号和推送状态，并明确区分本地预览、GitHub 源码同步与服务器部署。

当前交付边界：注册及相关 UI 调整仍按用户最近选择先本地验收。GitHub 推送不等于上线；服务器部署按后续明确指示执行，已获得的授权不需要重复询问。本协议整理本身不修改页面，也不触发部署。

## 12. 后续维护

- 用户新增明确偏好或纠正旧规则时，更新对应条目，保留最新有效要求，避免相互矛盾的规则并存。
- 分清“用户明确要求”“参考图归纳”和“实现/验收方法”，不能把开发者个人选择包装成用户长期偏好。
- 新页面可以继承通用色彩、质感和克制的排版，但页面专属构图、字段、文案和动画应依据该页面的新要求。
- 项目 [AGENTS.md](AGENTS.md) 提供本协议入口；本文件是本工作区 UI 偏好的集中维护位置。

## 13. 后台信息层级与页面顶部（2026-09-19 补充）

- 后台页面顶部只保留页面主标题、真正有用的数量状态和可操作入口。禁止使用“组织与权限”“数据空间”“现场作业”等装饰性眉题、分类小字或无功能标签。
- 页面主标题区域应紧凑，不能在导航下方留下大块空白。必要的使用说明统一折叠，并与标题同一行收纳，不得另占一整行。
- 静态说明不能做成按钮、选项卡或输入框的外观。没有操作价值且不影响任务理解的说明直接删除；有用提示放入折叠说明或字段占位文字。
- 表格标题、表头、核心数据和操作按钮必须形成明显层级，不能全部缩成近似字号。表头和业务数据应在常见桌面距离下直接可读，时间等次要信息只能适度减弱，不能小到需要凑近辨认。
- 账号管理等多业务页面按真实对象直接命名为“账号、品牌、工厂、邀请码”，不再叠加“成员、生产组织、合作方注册”等重复小标题。
- 个人设置等内容较少的页面应利用可用视口形成完整构图；可以使用开放分栏、分隔线和单一重点面板，不能把全部内容堆在顶部而让下半屏无意义空置。


## 14. 交互选中状态与图片性能（2026-09-20 补充）

- 导航、步骤、分页和页签的“当前项”必须清楚但克制：使用浅薄荷底、深绿文字、细边框或短下划线，不得出现贯穿导航高度的深色竖方块。
- 选中形状应顺着文字横向展开，保持足够内边距和圆角；不能只包住单个字形成狭窄竖块，也不能依靠低对比度颜色区分状态。
- 悬停、当前、键盘焦点和按下状态应可区分。当前项可持续显示，悬停只做轻量反馈，键盘焦点必须有清晰轮廓。
- 深绿色实底保留给主要操作按钮和明确的确认动作；导航选中、步骤选择、分页当前页等状态不用大面积深色填充。
- 后台界面保持已确认的明亮薄荷色体系，不随系统深色模式变成黑灰色块。
- 前端图片按实际展示尺寸提供资源：小头像和图标不得直接下载数百像素的大原图；大幅参考图优先使用经过视觉校验的 WebP，并保留必要分辨率。
- 同一页面重复引用同一图片时应复用相同 URL，让浏览器只下载一次；关键首屏图可预加载，非首屏图片采用延迟加载和异步解码。
- 每次替换静态资源或样式都更新资源版本参数，避免浏览器继续使用旧缓存而让用户看到已经修复的界面。


## 15. 入口网格与整页加载（2026-09-20 补充）

- 快捷入口、工作选择等同类网格必须利用整行宽度；最后一行数量不足时应自动均分剩余空间，不能把格子只堆在左侧而在右侧留下无意义空白。
- 桌面端优先形成完整的三列或四列行，平板与手机按可用宽度降为三列或两列；调整列数时同时保持图标、文字、分隔线和点击区域对齐。
- 首次进入任何页面时，在样式、字体、图片和布局完成前只显示居中的原地行走小象。小象必须直接使用 `UI设计样图/ChatGPT Image 2026年9月19日 13_42_43.png` 中的原始象形标志，不得凭轮廓重新绘制；头背、长鼻、白色圆眼和白色弧形耳线保持原图像素，只有从同一原图裁出的前后腿可以交替迈步。加载画面不显示“正在加载”“正在处理”等可见文字，只用小象动作表达状态。页面内容完成后整页一次性出现。
- 首屏加载层使用不透底的浅薄荷背景；页面内请求、跳转和数据加载继续复用同一只小象。加载失败必须有超时兜底，不能把页面永久锁死。
- 打印模式不显示加载层；减少动态效果偏好下保留可辨认但更舒缓的行走节奏。

## 16. 只读状态、后台导航与移动端操作（2026-09-23 补充）

- 只读体验账号的权限说明不能在每一页重复占用一整行；顶栏只显示克制的“只读模式”入口，完整说明按需展开。只读限制必须继续由服务端保证，不能因视觉收纳而放开写操作。
- 桌面端顶部导航的当前项和悬停状态只使用文字颜色与短下划线，不再叠加圆角矩形底框。键盘焦点仍须保留清楚可见的轮廓。
- 只读账号遇到不能执行的业务动作时，不显示看似可点击却完全无响应的按钮。可提供查看详情等只读操作；真实处理动作只对有权限的账号显示并保持可用。
- “我的”页面入口图标使用有对比度的浅色底与深色线条，不能出现白色图标落在透明或浅色背景上而看不清的情况。
- 个人设置等短内容页面使用与全站一致的薄荷绿半透明表面和克制字号，在桌面与常见手机视口中完整显示，不产生页面级滚动。
- 表格操作按钮中的短文案始终横向排布，禁止因列宽压缩把“启用”等两字拆成竖排；移动端长表格在内容容器内滚动，不能造成整页横向溢出。
